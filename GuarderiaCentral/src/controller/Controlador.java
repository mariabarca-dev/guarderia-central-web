package controller;

public interface Controlador {

    // Método de arranque/autenticación tal cual lo tenés definido en LoginController
    void login(String nombreUsuario, String claveIngresada);

}